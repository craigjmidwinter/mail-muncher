// Package config loads, validates, and resolves the mail-muncher YAML
// configuration file: provider credentials, rule definitions, sink targets,
// and any file paths referenced from within them.
package config
